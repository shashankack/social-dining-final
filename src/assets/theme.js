import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#B55725",
    },
    secondary: {
      main: "#ffffff",
    },
    background: {
      default: "#000000",
    },
    text: {
      primary: "#FFFFFF",
      secondary: "#B0B0B0",
    },
  },
});

export default theme;
